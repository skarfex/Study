CREATE TABLE IF NOT EXISTS ang_semenova_ram_location (
                id INT PRIMARY KEY,
                name VARCHAR NOT NULL,
                type VARCHAR NOT NULL,
                dimension VARCHAR NOT NULL,
                resident_cnt INT NOT NULL);